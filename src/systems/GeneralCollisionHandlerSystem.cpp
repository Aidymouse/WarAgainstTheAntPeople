#include <systems/GeneralCollisionHandlerSystem.h>

void GeneralCollisionHandlerSystem::update(float dt, ECS *ecs) {
  // Handle collisions for global things in a non-specific way
  // Kind of like how ECS is supposed to work!
}
